---
phase: 06-teacher-dashboard-and-roster-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
  - "attendance v 2.5.html"
autonomous: true

must_haves:
  truths:
    - "Teacher sees a dashboard with navigation between sections (attendance, roster, seating, settings)"
    - "Existing attendance view (period summary, absent list, group assignments) is preserved and functional"
    - "Timer fullscreen feature still works exactly as before"
    - "Kiosk mode still works exactly as before (sign-in, group display, PIN exit)"
    - "Navigation between dashboard sections does not reload the page"
  artifacts:
    - path: "public/index.html"
      provides: "Dashboard shell with navigation wrapping existing admin panel"
      contains: "dashboard-nav"
  key_links:
    - from: "dashboard navigation"
      to: "section panels"
      via: "click handler shows/hides sections"
      pattern: "dashboard.*nav|section.*panel"
    - from: "kiosk mode"
      to: "admin panel (now dashboard)"
      via: "PIN entry transitions to dashboard"
      pattern: "showAdminPanel|showDashboard"
---

<objective>
Create the dashboard shell and navigation structure that wraps the existing admin panel into a proper teacher dashboard with section-based navigation.

Purpose: Every subsequent plan (seating config, roster management, onboarding, sync status) needs a navigation structure to plug into. This plan creates that foundation while preserving all existing functionality.

Output: public/index.html with dashboard layout containing navigation tabs/sidebar and the existing attendance view as the default section.
</objective>

<execution_context>
@C:\Users\Jeremy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Jeremy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-teacher-dashboard-and-roster-management/06-CONTEXT.md
@public/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure admin panel into dashboard with navigation</name>
  <files>public/index.html</files>
  <action>
Refactor the existing admin-panel section of public/index.html into a dashboard layout with navigation. The current admin-panel div contains a 3-column grid with controls on the left and data on the right. Wrap this in a dashboard structure:

1. **Dashboard container** replaces the current admin-panel:
   - Full-width layout with a top navigation bar or sidebar (Claude's discretion per CONTEXT)
   - Navigation items: "Attendance" (default/active), "Roster", "Seating", "Settings"
   - Each nav item shows/hides a corresponding section panel
   - Only one section visible at a time

2. **Attendance section** (default view):
   - Move ALL existing admin panel content into this section: period selector, attendance summary, absent list, group assignments, export buttons, seating exceptions, timer
   - The timer fullscreen button must still work (it uses the global timer-fullscreen overlay which is OUTSIDE the admin panel — do not move or break it)
   - Preserve the existing "Return to Kiosk Mode" button — move it to a consistent location (e.g., top-right of dashboard header, or in the nav bar)

3. **Empty placeholder sections** for Roster, Seating, and Settings:
   - Each is a hidden div with a unique ID (e.g., dashboard-roster, dashboard-seating, dashboard-settings)
   - Show a simple "Coming soon" or empty state text for now
   - Plans 02, 03, and 04 will populate these

4. **Navigation logic**:
   - Add JavaScript to handle nav item clicks: hide all sections, show clicked section, update active nav state
   - Default to Attendance section on load
   - Store no state — just show/hide DOM elements

5. **Update showAdminPanel()** function:
   - Rename or update to show the dashboard container instead of the old admin-panel
   - Ensure PIN entry still transitions to the dashboard correctly
   - Ensure logout still works

6. **Dashboard header**:
   - Show teacher name (already exists as teacher-name-display)
   - Show current date (already exists as current-date)
   - Sign Out button (already exists as teacher-logout-btn)
   - "Return to Kiosk" button

7. **Preserve the following overlays** which live OUTSIDE the admin panel and must NOT be moved or broken:
   - timer-fullscreen (fullscreen timer overlay)
   - group-display-fullscreen (fullscreen group display)
   - login-screen, pin-setup-screen, pin-entry-screen

Design guidance:
- Use Tailwind classes consistent with existing app style (bg-gray-50, border-gray-200, text-gray-800, accent red-700)
- Navigation should be visually clear but not oversized — teachers will primarily use the Attendance tab
- Mobile-responsive: nav should stack or become a horizontal scrollable bar on small screens
  </action>
  <verify>
Open public/index.html in a browser. After logging in or entering PIN:
- Dashboard appears with navigation tabs
- Clicking each nav tab shows the corresponding section
- Attendance section contains ALL original admin panel content
- Timer fullscreen button works (opens timer overlay, Escape closes it)
- Group display fullscreen button works
- Return to Kiosk button works (goes to kiosk mode)
- Sign Out button works
- Period selector, export, and attendance summary still function
  </verify>
  <done>Dashboard shell with working navigation between 4 sections. Attendance section contains all existing admin functionality. All overlays and kiosk mode preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Sync source HTML and verify kiosk flow</name>
  <files>"attendance v 2.5.html", public/index.html</files>
  <action>
1. Copy public/index.html content to "attendance v 2.5.html" to keep them in sync (the source file should match the deployed file).

2. Verify the complete flow still works:
   - Login screen appears on first visit (no stored kiosk binding)
   - After login, PIN setup appears (for new users) or kiosk mode (for existing users)
   - From kiosk mode, entering the teacher PIN opens the PIN entry screen, correct PIN shows dashboard
   - Dashboard navigation works between all sections
   - "Return to Kiosk" from dashboard goes back to kiosk mode
   - Student sign-in still works in kiosk mode (enter 4-digit ID, get group assignment)
   - Sign out clears session and returns to login screen

3. Ensure no console errors on page load or during navigation transitions.
  </action>
  <verify>
Open public/index.html, open browser DevTools console. Walk through: login -> PIN setup or kiosk -> PIN entry -> dashboard -> navigate tabs -> return to kiosk -> sign in as student. No console errors except expected Firebase messages.
  </verify>
  <done>Source HTML synced. Full flow (login -> kiosk -> dashboard -> navigation -> kiosk) works without errors.</done>
</task>

</tasks>

<verification>
1. Open public/index.html in browser
2. Complete login flow, arrive at dashboard
3. Verify 4 navigation tabs are visible and clickable
4. Verify Attendance tab shows all original admin content
5. Verify timer fullscreen, group display fullscreen, and all overlays work
6. Verify Return to Kiosk and Sign Out work
7. Verify kiosk sign-in still works for students
8. No console errors during any transition
</verification>

<success_criteria>
- Dashboard shell with 4-section navigation (Attendance active by default)
- All existing admin panel features preserved in the Attendance section
- Kiosk mode, login, PIN flows unchanged
- Timer and group fullscreen overlays functional
- Source HTML and deployed HTML in sync
</success_criteria>

<output>
After completion, create `.planning/phases/06-teacher-dashboard-and-roster-management/06-01-SUMMARY.md`
</output>
