# Aeries Credentials
AERIES_USER=your_username
AERIES_PASS=your_password

# Required: Path to Firebase service account key (store outside the repo)
FIREBASE_KEY_PATH=/path/to/your/attendance-key.json
