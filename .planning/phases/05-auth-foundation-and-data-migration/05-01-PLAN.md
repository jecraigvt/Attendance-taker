---
phase: 05-auth-foundation-and-data-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrate-to-tenants.js
  - package.json
autonomous: false

must_haves:
  truths:
    - "Jeremy's existing attendance records appear at the new per-teacher path in Firestore"
    - "Record counts at the new path match the old path exactly — no records missing"
    - "Old data paths remain untouched as a safety net"
    - "Running the script a second time does not create duplicate records"
  artifacts:
    - path: "migrate-to-tenants.js"
      provides: "Idempotent data migration script"
      min_lines: 80
    - path: "package.json"
      provides: "firebase-admin dependency for Node.js migration"
      contains: "firebase-admin"
  key_links:
    - from: "migrate-to-tenants.js"
      to: "Firestore artifacts/{APP_ID}/public/data"
      via: "firebase-admin SDK read"
      pattern: "artifacts.*public.*data"
    - from: "migrate-to-tenants.js"
      to: "Firestore teachers/{uid}/data"
      via: "firebase-admin SDK write"
      pattern: "teachers.*data"
---

<objective>
Copy Jeremy's existing attendance data from the flat Firestore path to a per-teacher path structure, verify record counts match, and confirm the script is idempotent.

Purpose: The current data lives at `artifacts/{APP_ID}/public/data/...` with no teacher isolation. Every subsequent phase needs data under `teachers/{uid}/...`. Migration must happen first and be verified before any auth code ships (per STATE.md decision: migration-timing).

Output: A working migration script and verified data at the new Firestore path.
</objective>

<execution_context>
@C:\Users\Jeremy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Jeremy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-auth-foundation-and-data-migration/05-CONTEXT.md

Current Firestore path structure (from HTML source):
- Rosters: `artifacts/{APP_ID}/public/rosters/periods/{period}`
- Config: `artifacts/{APP_ID}/public/config`
- Attendance: `artifacts/{APP_ID}/public/data/attendance/{date}/periods/{period}/students/{studentId}`
- Period meta: `artifacts/{APP_ID}/public/data/attendance/{date}/meta/currentPeriod`

APP_ID = 'attendance-taker-56916'

Target per-teacher path structure:
- `teachers/{uid}/rosters/periods/{period}`
- `teachers/{uid}/config`
- `teachers/{uid}/attendance/{date}/periods/{period}/students/{studentId}`
- `teachers/{uid}/attendance/{date}/meta/currentPeriod`
- `teachers/{uid}/profile` (teacher metadata: name, creation date)

Jeremy's UID will be determined at migration time. The migration script should:
1. Create a Firebase Auth user for Jeremy (or use an existing one) and record the UID
2. Copy all data from old paths to new per-teacher paths under that UID
3. Leave old paths untouched
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration script and dependencies</name>
  <files>migrate-to-tenants.js, package.json</files>
  <action>
Create a Node.js migration script `migrate-to-tenants.js` that:

1. **Initialize Firebase Admin SDK** using a service account key (path from env var `FIREBASE_KEY_PATH` or default to the path in attendance-sync/.env.example). The Firebase project is `attendance-taker-56916`.

2. **Determine Jeremy's UID:**
   - Check if a teacher doc already exists at `teachers/` collection with Jeremy's info
   - If not, create a Firebase Auth user with email `jeremy@rollcall.local` (or find existing) using `admin.auth().createUser()` / `admin.auth().getUserByEmail()`
   - Store the UID for all subsequent writes

3. **Copy collections recursively:**
   - Source root: `artifacts/attendance-taker-56916/public/`
   - Target root: `teachers/{jeremy_uid}/`
   - Sub-paths to copy:
     - `rosters/periods/{periodId}` -> each period doc with its roster array
     - `config` -> single config doc
     - `data/attendance/{date}/periods/{period}/students/{studentId}` -> each student attendance doc
     - `data/attendance/{date}/meta/currentPeriod` -> period meta doc

4. **Idempotency:** Before writing each document, check if it already exists at the target path. Skip if it exists (log as "already migrated"). This makes the script safe to re-run.

5. **Count verification:** After migration, count documents at both old and new paths for each major collection (rosters, config, attendance). Print a comparison table showing old count vs new count. Flag any mismatches as ERRORS.

6. **Batch writes:** Use Firestore batch writes (max 500 per batch) for efficiency.

7. **Write a teacher profile doc** at `teachers/{jeremy_uid}/profile` with:
   ```json
   {
     "displayName": "Mr. Ramos",
     "email": "jeremy@rollcall.local",
     "createdAt": serverTimestamp,
     "migratedAt": serverTimestamp,
     "tardyConfig": {
       "method": "nth-student",
       "nthStudent": 5,
       "graceMinutes": 8
     }
   }
   ```

8. **Create `package.json`** at project root (if not existing) with `firebase-admin` as a dependency. Use `type: "module"` for ESM imports OR use CommonJS require — whichever is simpler.

Do NOT modify or delete any data at the old paths. Do NOT modify the HTML file or any other existing code.
  </action>
  <verify>
Run `node migrate-to-tenants.js` — script should:
- Print progress as it copies each collection
- Print a verification table at the end showing old vs new counts
- Exit with code 0 if all counts match, code 1 if any mismatch
- Running it again should show "already migrated" for all documents and same counts
  </verify>
  <done>
Migration script exists, installs cleanly with `npm install`, and is ready to run against live Firestore.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify migration against live Firestore</name>
  <what-built>
Migration script that copies all existing attendance data to per-teacher Firestore paths. The script is idempotent and produces a count verification report.
  </what-built>
  <how-to-verify>
1. Ensure your Firebase service account key is accessible (check path in script or set FIREBASE_KEY_PATH env var)
2. Run `npm install` then `node migrate-to-tenants.js`
3. Verify the count table shows matching numbers for rosters, config, and attendance records
4. Open Firebase Console -> Firestore -> check that `teachers/{uid}/` collection tree exists with your data
5. Confirm old path `artifacts/attendance-taker-56916/public/` still exists and is untouched
6. Run the script again to confirm idempotency (should show "already migrated" messages)
7. Open the kiosk app (rollcall.it.com) and confirm student sign-in still works (old paths still in use)
  </how-to-verify>
  <resume-signal>Type "approved" if counts match and kiosk still works, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] `migrate-to-tenants.js` exists and runs without errors
- [ ] `package.json` exists with firebase-admin dependency
- [ ] Script produces count verification table
- [ ] Script is idempotent (safe to re-run)
- [ ] Old Firestore paths remain untouched
- [ ] New `teachers/{uid}/` path contains all migrated data
- [ ] Teacher profile doc exists at `teachers/{uid}/profile`
</verification>

<success_criteria>
Jeremy's existing attendance data is copied to per-teacher Firestore paths with verified count parity. Old paths remain as safety net. The kiosk app continues to work unchanged on old paths.
</success_criteria>

<output>
After completion, create `.planning/phases/05-auth-foundation-and-data-migration/05-01-SUMMARY.md`
</output>
