---
phase: 04-tardy-logic-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-tardy-logic-review/TARDY-LOGIC-ANALYSIS.md
autonomous: true

must_haves:
  truths:
    - "Current tardy logic is fully documented with code references"
    - "All edge cases are identified and categorized"
    - "Dispute data is analyzed against current logic"
    - "Recommendation for new logic is provided with rationale"
    - "Bell schedule configuration location is documented"
  artifacts:
    - path: ".planning/phases/04-tardy-logic-review/TARDY-LOGIC-ANALYSIS.md"
      provides: "Complete analysis document"
      contains: "## Current Logic"
  key_links:
    - from: "TARDY-LOGIC-ANALYSIS.md"
      to: "attendance v 2.2.html"
      via: "code references"
      pattern: "statusForNow|fifthStudentTs|lateThreshold"
---

<objective>
Document and analyze the current tardy threshold logic ("5th student + 8 minutes") to identify all edge cases and determine why 52% of disputes are tardy-related.

Purpose: Provide a complete understanding of the current logic before implementing changes. This analysis becomes the foundation for the implementation plan.

Output: TARDY-LOGIC-ANALYSIS.md with documented logic, edge cases, and recommendation.
</objective>

<execution_context>
@C:\Users\Jeremy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Jeremy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@attendance v 2.2.html
@analyze_firebase.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Current Tardy Logic</name>
  <files>.planning/phases/04-tardy-logic-review/TARDY-LOGIC-ANALYSIS.md</files>
  <action>
Create TARDY-LOGIC-ANALYSIS.md with these sections:

## Current Logic

Document the `statusForNow()` function (lines 1252-1283 of attendance v 2.2.html):

1. **Algorithm:**
   - Get all sign-in logs for current period, sorted by timestamp
   - If fewer than 5 students signed in: return "On Time" for everyone
   - Get 5th student's sign-in timestamp (index 4)
   - Calculate lateThreshold = 5th student timestamp + 8 minutes
   - If current time > lateThreshold: return "Late"
   - Otherwise: return "On Time"

2. **Code Reference:** Include the exact JavaScript code block

3. **Where Used:**
   - Called in `handleSignIn()` when student signs in
   - Called in `manualCheckIn()` for admin check-ins
   - Status stored in Firebase and synced to Aeries

## Edge Cases

Document each edge case:

| # | Case | Current Behavior | Issue |
|---|------|------------------|-------|
| 1 | Fewer than 5 students in period | All marked "On Time" regardless of when they arrive | Small classes never have tardies |
| 2 | First 5 students arrive late (e.g., all at 8:40 for 8:30 class) | All marked "On Time", threshold set to 8:48 | Actual late students marked on-time |
| 3 | 5th student is very early (e.g., 8:20 for 8:30 class) | Threshold = 8:28, students arriving at 8:29 marked "Late" | On-time students marked late |
| 4 | Student signs in during passing period | Uses previous period's context if period not switched | Status could be wrong |
| 5 | No bell schedule reference | Logic ignores actual class start time | Threshold is relative, not absolute |

## Bell Schedule Configuration

Document the `bellSchedules` object (lines 616-624) which contains all configurable bell times:

**Location:** `bellSchedules` object in attendance v 2.2.html (lines 616-624)

**Structure:**
```javascript
const bellSchedules = {
  'Default': { '1': '08:30', '2': '09:32', ... },
  'Late Start': { '1': '09:30', '2': '10:22', ... },
  'Assembly': { ... },
  // Additional schedule types as needed
};
```

**Configuring Bell Times:**
- Bell schedule times are configurable by editing the `bellSchedules` object directly in the code
- Each schedule type (Default, Late Start, Assembly, etc.) has its own set of period start times
- To change a bell time: Edit the corresponding value in the bellSchedules object
- To add a new schedule type: Add a new key-value pair to bellSchedules

**Note:** Period start times ARE available in the system via `bellSchedules` but are NOT currently used in tardy calculation. The new logic will use these existing bell times.

## Dispute Analysis

Based on PROJECT.md context:
- 15 of 29 corrections (52%) are tardy disputes
- Students disputing tardies signed in at 8:27-8:34 AM for 8:30 AM class
- These times would be 3 minutes early to 4 minutes late by bell schedule
- But current logic doesn't check bell schedule at all

## Root Cause

The current logic creates disputes because:
1. It's relative (to 5th student) not absolute (to bell time)
2. A slow 5th student makes everyone after them "late"
3. A fast 5th student makes actual late students appear "on time"
4. The 8-minute buffer is arbitrary and doesn't match school policy

## Recommendation

**Proposed New Logic:**
```javascript
function statusForNow() {
  const periodStart = getPeriodStartTime(currentPeriod, scheduleName);
  const now = Date.now();
  const tardyThreshold = periodStart + TARDY_GRACE_MINUTES * 60 * 1000;

  if (now <= tardyThreshold) return 'On Time';
  return 'Late';
}
```

Where:
- `getPeriodStartTime()` reads from `bellSchedules[scheduleName][period]`
- `TARDY_GRACE_MINUTES` is configurable (suggest: 3-5 minutes based on school policy)

**Configuration Points:**
1. **Bell times:** Edit `bellSchedules` object to adjust period start times
2. **Grace period:** Change `TARDY_GRACE_MINUTES` constant to adjust tolerance

**Benefits:**
1. Deterministic: Same time = same status, always
2. Matches school policy: Based on actual bell times
3. No edge cases with small classes or early/late 5th students
4. Configurable grace period for school policy alignment
  </action>
  <verify>
File exists and contains all required sections:
- grep "## Current Logic" TARDY-LOGIC-ANALYSIS.md
- grep "## Edge Cases" TARDY-LOGIC-ANALYSIS.md
- grep "## Bell Schedule Configuration" TARDY-LOGIC-ANALYSIS.md
- grep "## Dispute Analysis" TARDY-LOGIC-ANALYSIS.md
- grep "## Root Cause" TARDY-LOGIC-ANALYSIS.md
- grep "## Recommendation" TARDY-LOGIC-ANALYSIS.md
  </verify>
  <done>TARDY-LOGIC-ANALYSIS.md exists with complete documentation of current logic, all edge cases identified, bell schedule configuration documented, dispute analysis, and recommendation for bell-schedule-based logic.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Analysis Against Codebase</name>
  <files>.planning/phases/04-tardy-logic-review/TARDY-LOGIC-ANALYSIS.md</files>
  <action>
Cross-reference the analysis document against actual code to ensure accuracy:

1. Verify `statusForNow()` location and exact line numbers in attendance v 2.2.html
2. Verify `bellSchedules` object structure and all schedule types
3. Verify `handleSignIn()` calls `statusForNow()`
4. Verify `manualCheckIn()` calls `statusForNow()`
5. Check if there are any other places where status is determined

Add a "## Code Verification" section to TARDY-LOGIC-ANALYSIS.md:

## Code Verification

| Function | File | Lines | Verified |
|----------|------|-------|----------|
| statusForNow() | attendance v 2.2.html | 1252-1283 | [x] |
| bellSchedules | attendance v 2.2.html | 616-624 | [x] |
| handleSignIn() uses statusForNow | attendance v 2.2.html | 1396 | [x] |
| manualCheckIn() uses statusForNow | attendance v 2.2.html | 1628 | [x] |

Update any line numbers or details if they differ from the analysis.
  </action>
  <verify>
TARDY-LOGIC-ANALYSIS.md contains "## Code Verification" section with line number references that match actual file content.
  </verify>
  <done>Analysis document is verified against actual codebase with correct line numbers and function references.</done>
</task>

</tasks>

<verification>
1. TARDY-LOGIC-ANALYSIS.md exists in phase directory
2. Document contains all seven required sections (including Bell Schedule Configuration)
3. Edge cases table has at least 5 documented cases
4. Bell schedule configuration location and edit instructions documented
5. Code verification section confirms line numbers
6. Recommendation section includes proposed new logic
</verification>

<success_criteria>
- Complete analysis document ready for implementation planning
- All edge cases from Phase 4 requirements are documented
- Bell schedule configuration documented (location: bellSchedules object, editable in code)
- Clear recommendation with code example for new bell-schedule-based logic
- Cross-referenced against actual codebase
</success_criteria>

<output>
After completion, create `.planning/phases/04-tardy-logic-review/04-01-SUMMARY.md`
</output>
